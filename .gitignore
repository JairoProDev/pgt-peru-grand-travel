# ds_store es un archivo que se crea en cada directorio que se crea en el sistema operativo macOS
.DS_Store
# __pycache__ es un directorio que se crea en cada proyecto Python
__pycache__/
*.pyc
.venv/
venv/

# POC Next.js — repo propio (pgt-poc / pgt-web), no versionar aquí
pgt-poc/
.env
.env.*
*.log
.canvas.data.json

# Secretos y credenciales — NUNCA subir
**/secretos/
**/secrets/
.secrets/*
!.secrets/README.md
!.secrets/.gitkeep
*Accesos*
*accesos*
*.xlsx
*.xls
*password*
*credencial*
*2fa*
*totp*
*.pem
*.key
id_rsa*
